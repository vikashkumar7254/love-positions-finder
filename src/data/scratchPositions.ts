export type ScratchPositionItem = {
  id: string
  title: string
  image: string
}

// No default positions - all positions managed through admin panel
export const scratchPositions: ScratchPositionItem[] = []
